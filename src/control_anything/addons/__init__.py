"""Addons: optional capability, each declared in ``data/addons.yml``.

An addon may import core and the addons it declares under ``extends``, plus the packages
it declares under ``requires``. Nothing else.
"""
