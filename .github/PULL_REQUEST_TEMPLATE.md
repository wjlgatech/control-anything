## What changed

<!-- One line: what, and why. -->

## The gate

- [ ] `make check` is green
- [ ] If I added or changed a **claim**, its assumption ledger is complete, and every
      `monitored` / `discharged` assumption **names its check**
- [ ] If I added a **work**, it has a year and a resolvable `doi` / `arxiv` / `url`, and
      `verified` reflects whether I actually resolved it against a primary source
- [ ] If I added a **person**, they own exactly one concept that exists
- [ ] If I tagged a **bridge** as `rigorous`, I can name the theorem
- [ ] Any number I wrote into prose is generated from `data/` (`make readme`), not typed

## What the gate said

<!-- Paste the relevant lines from `python3 scripts/ca.py gate` or `make stats`.
     If you changed a claim, show the verdict before and after. -->

```
```
